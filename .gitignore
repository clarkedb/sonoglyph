node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# mprocs (`pnpm dev`) diagnostic log — written at repo root on crash.
mprocs.log

# Next.js (website/)
.next/
out/
next-env.d.ts
website/.impeccable/hook.cache.json

# Cargo build output. Cargo.lock is committed (application tree, not a
# library-only one).
/target/

# wasm-pack output for @sonoglyph/dsp-wasm — generated by `build:wasm`.
packages/dsp-wasm/pkg/

# DSP benchmark gate run artifacts (bench-baselines/ is committed; these aren't).
# Written at the repo root during `.github/workflows/bench.yml`.
/bench-results/
/bench-report.md
/current-*.json
/payload.json
